# =============================================================
# NOERMS Environment Variables — Copy to .env and fill values
# Used by docker-compose.yml. NEVER commit your real .env to git.
# =============================================================
# Database
DB_NAME=noermsbrea
DB_USER=posdfdtgres
DB_PASSWORD=noerms_secure_2024
DB_PORT=54bggdgd32

# JWT (must be 64+ characters)
JWT_SECRET=noerms-default-secret-key-university-buea-cef476-2024-at-least-64-chars
JWT_EXPIRATION=86400000

# Frontend port (host machine)
FRONTEND_PORT=8000

# Redis — only needed if you uncomment the redis service in docker-compose.yml
# REDIS_PASSWORD=noerms_redis_2024
